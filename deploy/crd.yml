apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: pilotscans.harbor.vmware.com
spec:
  group: harbor.vmware.com
  versions:
    - name: v1
      served: true
      storage: true
  scope: Namespaced
  names:
    plural: pilotscans
    singular: pilotscan
    kind: PilotScan
    shortNames:
    - ps

